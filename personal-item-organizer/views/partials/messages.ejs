<%/* 
  消息通知已被禁用

  if(typeof errors !== 'undefined' && errors.length > 0) { 
    // 错误消息
  } 

  if(success_msg !== '') {
    // 成功消息
  }

  if(error_msg !== '') {
    // 错误消息
  }

  if(error !== '') {
    // 错误消息
  }
*/%>